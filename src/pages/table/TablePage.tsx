// react
import { FC } from 'react';
// ui
import { UserList } from '@/widgets/ui/user/UserList/UserList';

interface TablePageProps {}

export const TablePage: FC<TablePageProps> = ({}) => {
  return <UserList />;
};
