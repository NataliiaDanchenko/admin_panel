// config router
import { AppRouter } from './config/route/AppRouter';
// mui
import { Box } from '@mui/material';

const App = () => {
  return (
    <Box>
      <AppRouter />
    </Box>
  );
};

export default App;
